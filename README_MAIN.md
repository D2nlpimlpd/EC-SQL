# BoyueSQL Main Entrypoint

The project-level README is `readme.md`.

For the generic Text-to-SQL migration, Spider2 dataset setup, Linux service
startup, server experiment matrix, and current local evidence, use:

- `readme.md`
- `SERVER_RUNBOOK.md`
- `GENERALIZATION_PLAN.md`

The old Oracle-only documentation has been removed from this file so the main
entrypoint no longer advertises a private deployment as the default target.
